# ONE UP 大会エントリー — 実機テスト手順書

最終更新: 2026-07-02

本番相当の環境（Supabase 本番プロジェクト + Vercel デプロイ or `npm run dev`）で、
以下のシナリオを上から順に確認する。チェック欄 `[ ]` を `[x]` にして進める。

---

## 0. 事前準備

- [x] Supabase に `supabase/schema.sql`（および `migrations/0001_entry_phone.sql`）適用済み
- [x] `entries` テーブルに `phone` カラムがある / `user_profiles` テーブルが無い
- [x] `.env`（またはVercel環境変数）が設定済み
  - `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`
  - `VITE_LIFF_ID`
  - `VITE_ADMIN_PASSWORD`
  - `LINE_CHANNEL_ACCESS_TOKEN`
  - `ADMIN_LINE_USER_ID`（自分への通知を試すなら必須）
  - `CRON_SECRET`
- [x] `npm run build` がエラーなく通る

> テスト用データを事前投入したい場合は、管理画面（§4）で開催日を調整した大会を作ると楽。
> 受付期限テスト用に「開催7日以上先」「開催6日以内」の大会を1件ずつ用意しておく。

---

## 1. 起動・LIFF

- [x] LINEアプリからLIFF URLを開くと、プロフィール登録画面が**出ずに**いきなり「大会を選択」画面が表示される
- [x] ヘッダーが「ONE UP」でテーマカラーが**ピンク**になっている
- [x] （ローカル確認用）`VITE_LIFF_ID` 未設定 or `mock` の場合、ダミーユーザーで大会選択画面まで進める

---

## 2. エントリー正常系

- [x] 「大会を選択」に、有効な大会が年度ごとにグループ表示される
- [x] 受付中の大会をタップ → 入力画面へ進む
- [x] 入力画面に **チーム名 / 代表者名 / 電話番号** の3項目がある（すべて必須マーク `*`）
- [x] LINEアカウント（表示名・アイコン）が表示される
- [x] 3項目を正しく入力 →「確認へ進む」→ 確認画面に大会名・年度・開催日・会場・チーム名・代表者名・**電話番号**・申込者が表示される
- [x] 「エントリーを確定する」→ 完了画面に受付番号が表示される
- [x] Supabase の `entries` に新規行が入る（`phone` も保存されている）

---

## 3. エントリー異常系・制限

### 3-1. 入力バリデーション
- [x] チーム名が空 → エラー表示され進めない
- [x] 代表者名が空 → エラー表示され進めない
- [x] 電話番号が空 → エラー表示
- [x] 電話番号が不正形式（例: `abc`、`123`）→ 形式エラー表示
- [x] 電話番号 `090-1234-5678` / `09012345678` → どちらも通る

### 3-2. 1ユーザー1年度1大会
- [x] 同一ユーザーで、ある年度の大会にエントリー済みの状態で再度アプリを開く
- [x] 大会選択画面上部に「◯◯年度は既にエントリー済み」の案内が出る
- [x] その年度の大会は「エントリー済み年度」バッジ付きで選択不可
- [x] 別年度の大会は引き続きエントリー可能
- [x] （すり抜け確認）確定処理でも「既にエントリー済み」エラーになる

### 3-3. 受付期限（開催7日前まで）
- [x] 開催まで **7日以上先** の大会 → 「受付中」で選択可
- [x] 開催まで **6日以内 / 当日 / 過去** の大会 → 「**受付終了**」バッジ付きで選択不可、注記表示
- [x] （境界）開催がちょうど7日後の大会 → 受付**可能**
- [x] （境界）開催が6日後の大会 → 受付**不可**

### 3-4. 定員（capacity）
- [x] 出場枠 = 申込数の大会 → 「満員」バッジ・選択不可
- [x] 残り枠が2以下の大会 → 「残りN枠」バッジ表示
- [x] 定員ちょうどで確定しようとすると「定員に達しています」エラー

---

## 4. 通知（LINE）

> `VITE_LIFF_ID` が本番（mockでない）ときのみ `/api/notify` が呼ばれる。

- [x] エントリー確定後、**申込者本人**のLINEに受付控えメッセージが届く
- [x] メッセージに大会名・年度・開催日・会場・チーム名・受付番号が含まれる
- [x] `ADMIN_LINE_USER_ID` 設定時、**管理者本人**にも申込通知が届く
- [x] `ADMIN_LINE_USER_ID` 未設定でも、申込者への通知・エントリー自体は成功する（エラーで止まらない）

---

## 5. 管理画面（/admin）

- [x] `/admin` を通常ブラウザで開くとパスワード画面が出る
- [x] 誤ったパスワード → 「パスワードが違います」
- [x] 正しいパスワード（`VITE_ADMIN_PASSWORD`）→ 管理画面に入れる
- [x] リロードしても認証が維持される（同一タブ内）

### 5-1. 大会管理
- [x] 新規大会を作成 → 一覧に追加され、Supabase `tournaments` に反映
- [x] 大会を編集（名称・開催日・会場・定員など）→ 反映される
- [x] 公開/非公開を切替 → 非公開の大会はユーザー側「大会選択」に出ない
- [x] 大会を削除 → 紐づくエントリーも消える（CASCADE）

### 5-2. エントリー一覧
- [x] 大会を選ぶと、その大会のエントリー一覧が申込順に出る
- [x] 各エントリーにチーム名・代表者名・**電話番号**が表示される
- [x] キャンセル操作 → `status` が `cancelled` になり、一覧から外れる
- [x] キャンセル後、その枠が空くので該当ユーザーは再エントリー可能になる

---

## 6. keep-alive（cron / 任意）

- [x] `GET /api/keep-alive` に `Authorization: Bearer <CRON_SECRET>` 無しでアクセス → 401
- [x] 正しい `CRON_SECRET` 付き → `{ ok: true, timestamp: ... }` が返る
- [x] Vercel の cron 設定（毎日 1:00）が有効になっている

---

## 7. リグレッション / 見た目

- [x] 各画面でピンクテーマが一貫している（ヘッダー・ボタン・バッジ・フォーカス枠）
- [x] スマホ幅で崩れない
- [x] 戻る/進むボタンでステップ移動しても入力内容が保持される
- [x] 完了 → もう一度エントリー（別年度）の導線が動く

---

## 不具合記録

| No. | 画面/操作 | 期待 | 実際 | 状態 |
|-----|-----------|------|------|------|
|     |           |      |      |      |
